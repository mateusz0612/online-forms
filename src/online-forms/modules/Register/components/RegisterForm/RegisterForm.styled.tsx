import styled from "styled-components";
import { FormLabel } from "libs/ui";

export const RegisterFormLabel = styled(FormLabel)`
  align-self: center;
  text-transform: uppercase;
  letter-spacing: 1px;
`;
